apiVersion: v1
kind: Service
metadata:
  name: {{ include "stackit-exporter.fullname" . }}
  labels:
    {{- include "stackit-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
    {{- include "stackit-exporter.selectorLabels" . | nindent 4 }}
